funcion en new event.html para el reminder dialog
$scope.displayReminderDialog = function() {
+        
+      clearErrors();
+      
+      if ($scope.allValid()) {
+          $rootScope.$emit('openReminderEvent');
+      }
+  	}